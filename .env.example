# Environment Configuration Example

# Database Connection (PostgreSQL or SQLite fallback)
DATABASE_URL=postgresql://burnguard:burnguard123@localhost:5432/burnguard_db

# Model Paths
MODEL_DIR=models
DATA_RAW_DIR=data/raw

# Hybrid Risk Scoring Engine Weights
ANOMALY_WEIGHT=0.40
EARLY_DRIFT_WEIGHT=0.25
PREDICTED_RISK_WEIGHT=0.25
LOT_DEVIATION_WEIGHT=0.10

# Frontend API Endpoint
VITE_API_URL=http://localhost:8000/api
